# Goal

**Status**: paused
**Policy**: goal
**Auto-continue**: on
**File**: `20260823004806-wc3d26.md`
**Pause reason**: main model recovery — retrying in 60m (main model unknown — Stream ended without finish_reason — 继续：Cardinals 与 Commanders 全队深度页：)
**Agent suggests**: The provider failure is being retried automatically with bounded backoff and an extra :00:30 probe after each hour starts; configured fallback models are tried in order. /goal resume retries immediately; /goal cancel stops it.
## Provider diagnostic (forensics)

```text
Stream ended without finish_reason — 继续：Cardinals 与 Commanders 全队深度页：
```


## Objective

> 由主 agent 亲自完成（不派发子代理）：将 billsschedule2026 的 44 个字数<1500 的趋势新词页（src/app/<slug>/page.tsx）逐页扩写至 ≥1500 词（目标 1500~2500）。每页动笔前先用 scripts/multi-engine-search.py 查实该主题最新事实（≥2 源一致才落笔，严禁编造），并对照 INTENT-DECOMPOSITION-50PAGES.md 补齐用户微意图章节（新增 H2/H3、数据表、对比分析）；遵循 AGENTS.md 写作工作流与 B/R 暗黑 UI；保留已有结构件（RelatedTrendPages、第2张WebP图、Sources 区块、权威外链≥3、面包屑、JSON-LD）不删；已达标 6 页不动；不碰 layout.tsx/首页/about/privacy/terms。完成后构建+审计双门禁绿 → git 提交 → 推送 main（CI 部署 Cloudflare Pages）→ 重跑 IndexNow（57 路由）确认 200。

## Verification contract

字数核验脚本（构建后统计 out/*.html 可见文本词数）输出：50 个趋势页全部 ≥1500 词，<1500 的页数为 0
npm run build 退出码 0 且全部路由静态预渲染
python3 scripts/check-seo-audit.py 输出「汇总: ERROR 0」
git status 干净且 git log origin/main 包含本次扩写提交（git rev-list origin/main..HEAD 为空，即已推送）
git diff 确认已达标 6 页及 layout.tsx、首页 page.tsx、about/privacy/terms 无改动
IndexNow 提交脚本返回 HTTP 200 或 202（urlList=57 条）
